// Autogenerated by Mustache.
// Don't change the generated file manually, only the template

import java.util.*

{{#events}}
// {{ description }}
data class {{ name }}Event(
    {{#fields}}
    val {{ . }}: String{{^last()}},{{/last()}}
    {{/fields}}
) {
    val name: String = "{{ name }}"

    val data: Map<String, String>
        get() = mapOf(
            {{#fields}}
            "{{ . }}" to {{ . }}{{^last}},{{/last}}
            {{/fields}}
        )
}

{{/events}}
